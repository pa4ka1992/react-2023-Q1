import { type FC } from 'react';

import AppRouter from '@/router/Router';

import './App.scss';

const App: FC = () => {
  return (
    <div>
      <AppRouter />
    </div>
  );
};

export default App;

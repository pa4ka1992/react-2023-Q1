import { ROUTE } from './_constants';

const routes = [
  {
    path: ROUTE.home,
    // element: < />,
  },
  {
    path: ROUTE.about,
    // element: < />,
  },
  {
    path: ROUTE.notFound,
    // element: < />,
  },
];

export default routes;
